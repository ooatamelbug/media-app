_format_version: "3.8"
_transform: true

services:
  - host: mockbin.org
    name: auth_service
    port: 80
    protocol: http
    routes:
      - name: login_route
        paths:
          - /signin